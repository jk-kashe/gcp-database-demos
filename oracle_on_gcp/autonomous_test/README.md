# Oracle Autonomous Database @ GCP Test

This project deploys the `autonomous_at_gcp` module on top of the `landing-zone` module to verify functionality.

## Usage

1.  Run `make apply`.
2.  Provide the requested variables (Project ID, Billing Account, Region, Zone).
